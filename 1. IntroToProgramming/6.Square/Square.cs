using System;

class Square
{
    static void Main()
    {
        int firstNumber = 12345;
        double resulstSquare = Math.Pow(firstNumber, 2);

        Console.WriteLine("The square of 12345 is equal to: " + resulstSquare);
    }
}